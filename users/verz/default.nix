{home, ...}: {
  imports = [
    "${home}/services/kanata"

    "${home}/shell/zsh"
    "${home}/shell/starship"

    "${home}/desktop/hyprland"

    "${home}/apps/kitty"
    "${home}/apps/spotify"
  ];
}
